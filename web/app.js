const money = value => new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:2}).format(value);
const initials = name => name.split(' ').map(part=>part[0]).slice(0,2).join('');
async function loadDashboard(){const response=await fetch('/api/dashboard');if(!response.ok)throw new Error('Falha ao carregar');const data=await response.json();document.querySelector('#servicesBody').innerHTML=data.services.map(service=>`<tr><td>${service.client}<small class="service-name">${service.service}</small></td><td><span class="person"><span class="mini-avatar">${initials(service.professional)}</span>${service.professional}</span></td><td>${service.hours}h</td><td>${money(service.hours*service.rate)}</td><td><span class="status ${service.status==='Concluído'?'done':service.status==='Aguardando'?'wait':'progress'}">${service.status}</span></td><td><button class="more" aria-label="Mais opções"><i data-lucide="more-horizontal"></i></button></td></tr>`).join('')}
const toast=document.querySelector('#toast');document.querySelector('#newService').addEventListener('click',()=>{toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2500)});document.querySelector('#mobileMenu').addEventListener('click',()=>document.querySelector('#sidebar').classList.toggle('open'));loadDashboard().catch(()=>{document.querySelector('#servicesBody').innerHTML='<tr><td colspan="6">Não foi possível carregar os serviços.</td></tr>'});
