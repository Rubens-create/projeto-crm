const money=value=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(value);const form=document.querySelector('#serviceForm');const rows=document.querySelector('#catalogRows');
function render(options){rows.innerHTML=options.map(o=>`<div class="catalog-row"><span class="service-option-icon"><i data-lucide="briefcase-business"></i></span><div><strong>${o.name}</strong><small>${o.description||'Sem descrição'}</small></div><b>${money(o.rate)}<small>/ hora</small></b><span class="active-badge"><i data-lucide="check"></i> Ativo</span></div>`).join('');lucide.createIcons()}
async function load(){render(await fetch('/api/admin/services').then(r=>r.json()))}form.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));data.rate=Number(data.rate);const response=await fetch('/api/admin/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(response.ok){form.reset();render(await response.json())}});load();
