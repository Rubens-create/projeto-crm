# Servidor
PORT=8080

# PostgreSQL (banco principal)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui
DB_NAME=crm_db
DB_SSLMODE=disable

# Ou use DATABASE_URL para override completo:
# DATABASE_URL=postgres://user:pass@host:5432/dbname?sslmode=disable
